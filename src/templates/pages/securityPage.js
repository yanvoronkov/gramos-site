// Страница безопасности и надежности GramOS
const { MOCKS } = require('../../data/mocks');
const { card, faq, ctaBand, pageHero } = require('../components');
const { SECURITY_FAQS } = require('../../data/faqs');

function renderSecurityPage() {
  return `
  ${pageHero({
    crumbs: [['Главная', '/'], ['Безопасность', '']],
    eyebrow: 'Безопасность и доверие',
    title: 'Данные клиентов, токены и платежи под контролем',
    lede: 'GramOS работает с деньгами и перепиской ваших клиентов, поэтому изоляция проектов, права доступа и корректная обработка платежей заложены в фундаментальную архитектуру.',
    mock: MOCKS.cabinet,
    second: 'Запросить детали',
    secondHref: '/contact/'
  })}

  <section class="section soft">
    <div class="wrap">
      <div class="grid g3">
        ${card({ i: 'layers', t: 'Изоляция проектов', d: 'Контакты, боты, каналы, платежи и сценарии принадлежат конкретному проекту. Данные разных клиентов и брендов гарантированно не пересекаются.' })}
        ${card({ i: 'team', t: 'Роли и доступы', d: 'Участник команды видит только то, что разрешает его роль: от просмотра аналитики до полного управления настройками.' })}
        ${card({ i: 'key', t: 'Хранение токенов', d: 'Токены ботов и ключи платёжных провайдеров хранятся в зашифрованном виде и не показываются в интерфейсе повторно.' })}
        ${card({ i: 'shield', t: 'Подписи webhook', d: 'Входящие уведомления от платёжных систем проверяются по криптографической подписи — подделать событие оплаты невозможно.' })}
        ${card({ i: 'check', t: 'Идемпотентность платежей', d: 'Повторная доставка webhook не приводит к двойному начислению, двойной выдаче доступа или повторному списанию.' })}
        ${card({ i: 'phone', t: 'Telegram initData', d: 'Авторизация в Mini App проверяется криптографически на сервере, а не на стороне клиента.' })}
        ${card({ i: 'note', t: 'Аудит действий', d: 'Изменения сценариев и ключевых настроек логируются: видно, кто и когда что поменял.' })}
        ${card({ i: 'globe', t: 'Изоляция виджета', d: 'Shadow DOM и изолированный контекст исполнения не дают виджету получить доступ к данным вашего сайта.' })}
        ${card({ i: 'card', t: 'Деньги напрямую', d: 'GramOS не является платёжным агентом: средства идут напрямую через вашего провайдера на ваш расчётный счёт.' })}
      </div>
    </div>
  </section>

  <section class="section">
    <div class="wrap" style="max-width:820px">
      <div class="center" style="margin-bottom:32px">
        <h2>Частые вопросы по безопасности</h2>
      </div>
      ${faq(SECURITY_FAQS)}
      <div class="callout">
        Раздел описывает инженерные принципы работы платформы и не заменяет юридические документы. 
        Точные юридические условия обработки персональных данных — в <a class="btn-link" href="/legal/privacy/">политике конфиденциальности</a>.
      </div>
    </div>
  </section>

  ${ctaBand('Нужны детали для службы безопасности?', 'Пришлём описание архитектуры, порядок обработки данных и ответим на вопросы вашей команды.')}
  `;
}

module.exports = { renderSecurityPage };
